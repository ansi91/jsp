<?xml version="1.0" encoding="UTF-8" ?> 
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="orm.mybatis.mapper.testMapper">
	<select id="testList" parameterType="map" resultType="string">
		SELECT TEXT FROM A
		WHERE TEXT=#{abcd}
	</select>
</mapper>